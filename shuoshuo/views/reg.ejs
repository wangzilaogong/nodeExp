<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login@wk private</title>
    <!--响应式，约束缩放-->
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <!--通知IE采用其支持的最新模式-->
    <meta http-equiv="X-UA-COMPATIBLE" content="IE=edge">

    <link rel="stylesheet" type="text/css" href="css/bootstrap.min.css">
    <script src="js/jquery.min.js"></script>
    <script src="js/bootstrap.min.js"></script>
    <script src="js/jquery-1.11.3.min.js"></script>
    <style>
        .container{
            width: 450px;
            height: 380px;
            background: #ffffff;
            position: absolute;
            left: 50%;
            top: 50%;
            transform: translate(-50%,-50%);
            border-radius: 10px;
        }
        #errorpage,#errorpage1{
            display: none;
        }

    </style>
</head>
<body class="bg-info">
<div class="container">
    <form role="form">
        <h3 class="text-center">欢迎注册</h3>
        <div class="form-group">
            <label for="username">用户名</label>
            <input type="text" id="username" placeholder="请输入用户名" class="form-control">
        </div>

        <div class="form-group">
            <label for="pass">密码</label>
            <input type="password" id="pass" placeholder="请输入密码" class="form-control">
        </div>


        <button type="button" id="zhuce" class="btn btn-success btn-block">OK</button>
        <a  class="btn btn-success btn-block" href="/login">取消</a>
    </form>
</div>
<div id="errorpage" class="alert alert-danger" role="alert">
    用户名被占用，请换一个.
</div>
<div id="errorpage1" class="alert alert-danger" role="alert">
    服务器error
</div>
<script type="text/javascript">
    $("input").focus(function(){
       $('#errorpage').fadeOut();
    });
    $("#zhuce").click(function () {
        $.post("/doreg",{
            "username":$('#username').val(),
            "password":$('#pass').val()
        },function(result){
            if(result=="1"){
                alert('注册成功，即将返回到首页！');
                window.location="/";
            }else if(result=="-1"){
                $('#errorpage').fadeIn();
            }else if(result== "-3"){
                $('#errorpage1').fadeIn();
            }
        });
    })


</script>
</body>
</html>